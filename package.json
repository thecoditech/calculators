{
  "private": "true",
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "scripts": {
    "lerna": "lerna",
    "bootstrap": "lerna bootstrap",
    "test": "lerna run test",
    "build": "lerna run build",
    "publish": "lerna publish --yes"
  },
  "devDependencies": {
    "@emmanuelgautier/renovate-config": "1.4.0",
    "cz-conventional-changelog": "3.3.0",
    "lerna": "4.0.0",
    "standard-version": "9.3.2"
  },
  "config": {
    "commitizen": {
      "path": "cz-conventional-changelog"
    }
  }
}
